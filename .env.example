# Multi-device sync (see docs/superpowers/specs/2026-06-15-komposvision-sync-design.md)
# Base URL of the FastAPI backend, e.g. https://api.example.com
EXPO_PUBLIC_API_BASE_URL=
# Plaintext device API key seeded in the `devices` table (sent as X-Device-Key)
EXPO_PUBLIC_DEVICE_API_KEY=
